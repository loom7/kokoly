<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Modell-Download beim ersten Start (ADR-0005). Sonst keine Netznutzung. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:label="Kokoly"
        android:allowBackup="false">

        <!-- Der TTS-Dienst: macht die App in den Systemeinstellungen unter
             "Text-in-Sprache-Ausgabe" wählbar. -->
        <service
            android:name=".service.KokolyTtsService"
            android:exported="true"
            android:permission="android.permission.BIND_TEXT_TO_SPEECH">
            <intent-filter>
                <action android:name="android.intent.action.TTS_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.speech.tts"
                android:resource="@xml/tts_engine" />
        </service>

        <!-- Pflicht-Begleiter des TTS-Vertrags (Stubs in M1, voll in M4). -->
        <activity
            android:name=".service.CheckVoiceData"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.speech.tts.engine.CHECK_TTS_DATA" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".service.GetSampleText"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.speech.tts.engine.GET_SAMPLE_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="true"
            android:label="Kokoly-Einstellungen" />
    </application>
</manifest>
